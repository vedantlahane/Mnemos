# === FILE: k8s/service.yaml ===

apiVersion: v1
kind: Service
metadata:
  name: mnemos-backend
  namespace: mnemos
  labels:
    app: mnemos-backend
spec:
  type: ClusterIP
  selector:
    app: mnemos-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP